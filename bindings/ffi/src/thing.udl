namespace thing {
    string p2tr_sign(u8 coin_type, string priv_hex, string tx_hex, string tx_prevouts_json);
    string p2pkh_sign(u8 coin_type, string priv_hex, string tx_hex);
};

interface HDWallet {
    constructor(u8 coin_type, string seed_hex);
    [Name=from_master_priv]
    constructor(string master_priv);
    string export_master_priv();
    string bip44_address();
    string bip86_address();
    string bip44_priv_hex();
    string bip86_priv_hex();
};
